# Replica fab DRC disposition

Status date: 2026-07-06.

Source report: `fab/gerbers/juku_routed-drc.json`, generated by
`kicad/report_fab_readiness.py` with KiCad 10.99 nightly. The `fab/` directory is
ignored, so this file is the tracked disposition record for the current main-board
fabrication review.

Machine package gate: `kicad/report_order_readiness.py` currently emits
`fab/gerbers/order-readiness.md` with status **ORDER READY**. The exact-count
waiver report is generated at `fab/gerbers/review-waivers.md`. Independent
Tracespace render evidence is generated at `fab/gerbers/external-gerber-review.md`
with top/bottom SVG and PNG review outputs under `fab/gerbers/review/tracespace/`.

## Summary

The routed replica board is electrically/routing clean for the current order gate:

| Gate item | Count | Disposition |
|---|---:|---|
| Clearance violations | 0 | Pass |
| Shorting items | 0 | Pass |
| Unconnected items | 0 | Pass |

The remaining 599 DRC findings are non-electrical fabrication-review items:

| DRC type | Count | Current disposition |
|---|---:|---|
| `courtyards_overlap` | 55 | Waivable after assembly-fit review |
| `pth_inside_courtyard` | 71 | Waivable after assembly-fit review |
| `silk_over_copper` | 199 | Cosmetic; fix only where assembly labels become unreadable |
| `silk_overlap` | 199 | Cosmetic; fix only where assembly labels become unreadable |
| `text_thickness` | 75 | Cosmetic/manufacturing readability; batch-fix if time allows |

Overall fabrication state is **order-ready by machine gate**: electrical,
routing, fabrication inventory, exact-count waiver, independent external-render,
power-trace envelope, and dual-config parts-BOM gates pass. The final order-time
step is visual/vendor review of the regenerated Gerbers.

The power-trace DFM envelope is tracked in
`docs/replica-power-trace-readiness.md`: 704 routed power segments are present,
377 are widened beyond the 0.20 mm freerouter baseline, no power segment is
below the routed baseline, and none exceeds the 1.00 mm clamp. KiCad DRC remains
the clearance authority.

The exact vendor upload/runbook layer is tracked in
`docs/replica-order-upload-runbook.md` and regenerated by
`python3 kicad/report_replica_order_upload_runbook.py`. It creates the ignored
`fab/gerbers/upload/juku-replica-gerbers-drill.zip` from the expected Gerber and
drill files and records the order-time vendor preview checks.

## Must review before order

No electrical, routing, footprint-library, copper-edge, or unwaived DRC classes
currently block fabrication package generation.

## Waivable after visual review

Courtyard and PTH-inside-courtyard findings are expected on this authenticity-first
2-layer recreation because the board intentionally follows the dense original
placement rather than modern courtyard spacing. Review the top offenders in KiCad
and in an independent Gerber viewer; waive if socket bodies, connectors,
decouplers, and mounting hardware have physical clearance.

Top repeated references from the current report:

| DRC type | Highest-repeat references |
|---|---|
| `courtyards_overlap` | D84, D95, D102, D76, C13, D93, C12 |
| `pth_inside_courtyard` | D102, D95, D84, C9, D2, D26, D93 |

Silkscreen and text findings are cosmetic unless they obscure assembly-critical
reference designators, polarity marks, connector orientation marks, or bring-up
test labels. Review these repeated clusters first:

| DRC type | Highest-repeat references |
|---|---|
| `silk_over_copper` | D95, E13, D84, D102, C9, D76, S3, S1 |
| `silk_overlap` | C1, C33, S3, S1, D23, D2, D25, D24 |
| `text_thickness` | R39, VD3, R19, R3, R38, VD5, C73 |

## Next actions

1. Review `fab/gerbers/external-gerber-review.md` plus the generated top/bottom
   PNGs, then check the courtyard and silkscreen clusters listed above in the
   vendor preview before ordering.
2. Run `python3 kicad/report_replica_order_upload_runbook.py` and upload only the
   generated `fab/gerbers/upload/juku-replica-gerbers-drill.zip` if the report is
   `READY`.
3. Regenerate `fab/gerbers/fab-readiness.md`,
   `docs/replica-power-trace-readiness.md`, and update this disposition if
   counts or decisions change.

## Resolved items

- `lib_footprint_issues`: resolved by vendoring `juku:CONN_X1`, `CONN_X2`,
  `CONN_X3`, `CONN_X8`, and `CONN_X9` under `kicad/juku.pretty/` and adding the
  project `kicad/fp-lib-table`. KiCad 10.99 nightly now resolves those connector
  footprints from tracked repo files.
- `copper_edge_clearance` and `silk_edge_clearance`: resolved by removing the two
  conflicting generated Edge.Cuts circles at `(104.0,251.4)` and `(300.3,138.1)`.
  Those read targets collided with D59/D34 copper in the current rectangular
  board model. They are deferred mechanical-overhang/fixture features until the
  exact non-rectangular outline can be re-read rather than manufactured as holes
  through routed copper.
- Review-only DRC classes: accepted by `kicad/report_review_waivers.py`, which
  fails if any waived count changes or if a new unwaived DRC type appears.
